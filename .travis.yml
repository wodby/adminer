language: bash

services:
  - docker

env:
  global:
    - BASE_IMAGE_STABILITY_TAG=4.10.0
    - ADMINER_VER=4.6.3 ADMINER_LANG=en TAGS=4.6,4,latest
    - ADMINER_VER=4.7.0 ADMINER_LANG=en TAGS=4.7,4,latest

script:
  - make && make test

after_success:
  - . "${TRAVIS_BUILD_DIR}/release.sh"
